# ルーカス音声常駐AI — TODO

プラン: `C:\Users\mahim\.claude\plans\vivid-seeking-nygaard.md`

## Step 0 — 土台確認
- [x] プロジェクトディレクトリ・venv 作成
- [x] 依存インストール（mcp / pywin32 / faster-whisper / sounddevice / numpy / soundfile）
- [x] STTモデル取得: `small`（疎通用・ロード検証済）＋ `kotoba-whisper-v2.0-faster`（日本語本命・7ファイル実体確認）

## Step 1 — 喋る（TTS）— ✅ 完了（2026-07-11）
- [x] `speak_server.py`（MCP・SAPI Haruka）作成
- [x] SAPI 単体で日本語が再生されるか確認（_make_voice が Haruka を選択・Speak 完走）
- [x] `claude mcp add --scope user lucas-speak` で登録（.claude.json）
- [x] `claude mcp list` で `lucas-speak √ Connected` 確認
- [x] ★まひろ確認: 実際に音が聞こえた（本人確認 2026-07-11「すげえ 聞こえたよ」）

## 参考資料からの取り込み（SuperWhisper/Aqua設計書・日本語ASR調査）
- [x] クリップボード保存→ペースト→復元パターンを cc_controller に実装（両巨頭と同方式）
- [x] STT本命を kotoba-whisper-v2.0-faster に確定（日本語×faster-whisper直結）
- [x] コールドスタート対策＝モデル事前ダウンロード実施
- [ ] 無音ハルシネーションフィルタ（test_listen に原型実装済み→lucas_voice に反映）

## Step 2 — 聞く＋CC制御 — ✅ 完了（2026-07-11）
- [x] `cc_controller.py` 作成（wt.exe `--suppressApplicationTitle` で固定タイトル "Lucas-CC" = identity 担保。他CCセッションへの誤爆防止）
- [x] マイク→whisper 疎通（「しゃべってみるわ」正解認識。マイク品質はまひろの耳で再生確認=クリア）
- [x] 日本語注入の往復証明: 「ファイルを書け」指示を注入→CC が5秒で「受信OK」ファイル生成
- [x] ライフサイクル: 起動→タイトル発見→/exit→ウィンドウ消滅 のフルサイクル検証
- [x] 実測処方箋の確立: 正規化＋内蔵VADオフ＋ビーム5／閾値0.005／ハルシネーションフィルタ

## Step 3 — 常時監視＋ライフサイクル（最終形）
- [x] `lucas_voice.py`（音量VAD 常駐 → kotoba STT → 意図分類 → CC制御）作成
- [x] `config.toml`（ウェイクワード・終了語・閾値・モデルを外出し）
- [x] `start_lucas.bat`（ダブルクリック起動）
- [x] speaking.lock で TTS/ビープ中の STT ミュート（エコー抑制。MCP speak と常駐側の両方が同じ lock を使用）
- [x] ロジック全ユニット検証パス（wake誤起動なし・長文中farewell誤終了なし・strip・normalize）
- [ ] ★E2E（まひろの声）: start_lucas.bat →「ルーカス」→ CC起動 → 数往復 →「バイバイ」→ 終了
- [ ] E2E で判明した調整（ウェイクワードの誤認識バリエーション追加・閾値・beam速度など）

## E2Eフィードバック改修（2026-07-11 まひろの初回E2E後）
- [x] **下書き＋「これでOK」送信方式**（自動送信廃止。「キャンセル」で破棄。ロジックテスト8本パス）
- [x] **毎ターンspeak**: ワークスペース `CLAUDE.md` に規約明文化＋speakツール説明を更新
- [x] **CCワークスペース = lucas-voice**（wt -d が効かないバグ発見→Set-Location方式で修正）
- [x] **ヘッドレス化**: `start_lucas_headless.vbs`（pythonw・ウィンドウなし）＋`stop_lucas.bat`＋log()のstdout無しガード
- [x] コールドスタート対策: 起動時ウォームアップ推論
- [x] 誤発火ゲート: 有声0.3秒未満は文字起こししない（'' 連発と5秒CPU浪費の対策）
- [x] 「認識されない」根治: 処理中の発話をキューに保持（無差別drainを廃止）
- [x] エコー抑制二重化: lock＋`last_speak_end` タイムスタンプ（文字起こし中のCC発話も破棄）
- [x] ハルシネーション追加: ごちそう/ごちそうさまでした
- [x] 安全装置: タイトル完全一致＋**前面化確認後にのみキー送信**（他ウィンドウ誤爆防止）＋env スクラブ起動
- [x] README.md 整備

## 検証メモ（2026-07-11）
- 私（Desktopアプリ）からのCC起動テストは環境変数継承（CLAUDE_CODE_CHILD_SESSION等）で「アプリの子セッション」になる——本番経路（リスナー=まひろの環境から起動）は無傷。詳細: troubleshooting.md 2026-07-11
- speak_server 直接呼び出しは `spoken` 確認済み。**MCP経由speakとワークスペースCLAUDE.md読込は、まひろの実環境E2Eで最終確認する**（私の経路では検証不能）

## ステータスバーUI（2026-07-11 まひろ要望・実装完了）
- [x] `ui.py` — 最前面フローティングピル（Aqua Voice風・Tokyo Nightテーマ・tkinterのみ）
- [x] 状態表示: loading/待機/聞いてる(脈動)/認識中/CC起動中/対話中/発話中/エラー＋下書き件数バッジ＋送信フラッシュ
- [x] lucas_voice に配線（UIメインスレッド＋リスナーworkerスレッド・COM初期化）
- [x] ドラッグ移動可・`[ui] enabled=false` で無効化可
- [x] 実物確認: PrintWindowキャプチャで待機/認識中の2状態を目視検証（GDI通常キャプチャはレイヤードウィンドウを写せない罠あり）
- [x] ロジックテスト8本 引き続きPASS

## 起動バグ修正（2026-07-11 まひろE2Eで発覚 → 同日修正・実測済み）
- [x] 0x80070002「" claude" が見つかりません」= wt.exe が「;」を自身の区切りに解釈する分割事故 → `launch_cc.ps1` に封じ込め（wt には -File で渡す）
- [x] launch_cc.ps1 が BOMなしUTF-8+日本語コメントで cp932 誤読→改行食いでコード行がコメント化 → BOM付きUTF-8 で再作成（先頭 EF BB BF 確認済み）
- [x] cwd 証跡ファイル `logs\last_cc_cwd.txt` を恒久実装（起動のたびに実 cwd を記録＝再発の機械検知）
- [x] フルサイクル再検証: 起動→cwd証跡 `C:\Users\mahim\lucas-voice` ✓→発見✓→終了✓
- [x] 教訓2件を troubleshooting.md に記録（未検証出荷／エンコーディング制約の再発）

## UI改修 第2弾（2026-07-11 まひろ要望・実装＋実測済み）
- [x] 待機中（idle）はバー非表示（タイムライン実測で表示/非表示の遷移確認）
- [x] ホバーで×ボタン表示→クリックでバーを閉じる（キャプチャで×描画確認。リスナーは継続）
- [x] ワークスペース lucas-voice 固定（launch_cc.ps1 のデフォルト値にも焼き込み。※まひろが見た「mahimで開く」は BOM修正前の 19:36 起動分＝修正済みを確認）
- [x] 二重起動ガード（Windowsミューテックス。実際に pythonw 2重稼働が起きていた→停止済み。A取得/B拒否をテスト）

## UI改修 第3弾（2026-07-11 まひろ要望・実装＋実測済み）
- [x] バーは「ルーカス」起動後のセッション中のみ表示（UIState.session フラグ。待機中は録音/認識中でも非表示＝周囲の音での点滅防止。例外はエラーとフラッシュのみ）
- [x] タイムライン実測: `.....(待機+環境音).....VVVV(起動〜対話〜発話)....(終了後).` を確認

## 文字起こしモード（2026-07-12 まひろ要望・実装＋実測済み）
- [x] グローバルホットキー起動の音声入力（Aqua Voice/Typress型）: `Ctrl+Shift+Space` → 話す → カーソル位置に貼り付け（Enterなし・フォーカス不変・クリップボード復元）
- [x] ホットキー空き実測: ctrl+alt+space ✗(1409他アプリ) / win+shift+space ✗ / **ctrl+shift+space ○** → デフォルト採用
- [x] 全チェーン実証: RegisterHotKey→合成キー押下→WM_HOTKEY→トグル発火2回 PASS
- [x] UI: dictate_wait/dictating/dictate_proc 状態（ティール色・セッション外でも表示）＋「貼り付けたよ ✓」フラッシュ
- [x] トグル/タイムアウト/ホットキー解釈のロジックテスト（計10本 ALL PASS）
- [x] cc_controller.paste_text 追加（フォアグラウンド不変・Enterなしのペースト専用関数）

## 「UIが出ない・キー無反応」障害の根治（2026-07-12・スモーク8/8 ALL PASS）
- [x] 真因1: 動いていたのは今朝0:43起動の**旧ビルド×2**（文字起こし機能なし）→ 停止
- [x] 真因2: 二重起動ガード素通り = `windll.kernel32.GetLastError()` 直呼びの値汚れ → `use_last_error=True`＋`ctypes.get_last_error()` に修正（troubleshooting.md 記録）
- [x] 真因3: stop_lucas.bat 不発 → ps1分離（stop_lucas.ps1）で堅牢化・実測PASS
- [x] Altダブルタップ実装（まひろ要望「altだけ」→単押しはOS予約で不可、Aqua流ダブルタップで実現）。**幽霊キー0xF0**（日本語KBの英数系が常時押下扱い）で全キー走査が永久トリップ→ベースライン方式で解決
- [x] モデルロード 173秒事件: HF Hubの更新チェック通信が犯人（レート制限）→ `local_files_only=True` 優先で3.1秒に
- [x] ウォームアップの完全無音入力はハルシネーション暴走で40秒超 → 微小ノイズ0.5秒に
- [x] 起動応答性: ホットキー登録とUIをモデルロードの**前**に（起動0秒で反応）
- [x] 一気通貫スモーク8項目 ALL PASS（登録即時/待機開始/バー存在/待機非表示/二重ガード/ダブルタップ開始/再タップキャンセル/停止）

## 文字起こしv2＋UI v2（2026-07-12 まひろFB4件・スモーク9/9 ALL PASS）
- [x] **カーソル消失バグ根治**: バーのdeiconifyがフォーカス強奪→キャレット消滅。WS_EX_NOACTIVATE|TOOLWINDOW付与（GetParentでは効かずGetAncestor(GA_ROOT)が正解・属性YESを実測）
- [x] **push-to-talk化**: Ctrl+Alt長押し中だけ録音・離すと確定（ダブルタップ/トグル方式は廃止）。他キー混入=ショートカット扱いでキャンセル・0.3秒未満無視
- [x] **クリップボード保持**: 文字起こし結果は常にクリップボードに残す（カーソル無し環境でも取り出せる。貼り付けも送出）
- [x] **認識ログUI**: バーホバーで≡ボタン→履歴ウィンドウ（時刻・モード色分け・新しい順・200件）
- [x] スモーク9/9: 実環境音声でPTT録音→認識→貼り付け＋クリップボードの全パイプライン動作を確認
- [x] ロジックテスト10本 ALL PASS（PTT状態遷移・hold_keys解釈に更新）

## 文字起こしv3（2026-07-12 まひろFB: 安定性・上限・速度・精度）
- [x] **クリックで録音死亡を根治**: 「他キー=キャンセル」を全撤去。止まるのは 離す/×/上限180s の3つだけ（録音しながらクリックで貼り付け先を選べる）
- [x] **×の意味を文脈化**: 録音中=録音キャンセル / 通常時=バーを閉じる
- [x] **録音上限の真実**: 文字起こし自体に上限なし（171s連続音声で最終文まで実測）。体感の「上限」の正体はクリックキャンセルバグ。暴走防止の自動確定180s＋残り20sからカウントダウンタイマーをバーに表示
- [x] **速度・精度**: 実測ベンチで既定を kotoba beam5 → **large-v3-turbo beam2 + cpu_threads8** に変更（RTF 0.65→0.44・一致率 81.4%→95.2%）。さらに**チャンク並行認識**（録音中に約20s毎・無音境界で裏スレッド処理）で、離した後の待ちは録音長に関係なく数秒に
- [x] condition_on_previous_text=False（長録音の反復・脱線抑止）
- [x] 検証: 合成注入テスト（171s実音声→チャンク12個→結合→貼り付けモック・一致率>85%・×キャンセル経路）+ ロジックテスト10本 ALL PASS
- [x] スモークに「本番リスナー稼働中なら殺さずABORT」ガード（2度の巻き添え事故の再発防止。troubleshooting.md記録）
- [ ] 既知の軽微課題: SAPI生成の検証音声が22.05kHz（16kHz想定に混入）——ベンチ絶対値は保守的な見積もり。実使用音声での体感確認はまひろのE2Eで

## グラスUI＋ダッシュボード（2026-07-12〜13 まひろ要望・実装＋オラクル検証済み）
- [x] **ダッシュボード** `dashboard.py`＋`web/dashboard.html`（pywebviewローカルアプリ・グラスモーフィズム・オーロラ背景・リキッドオーブ）: 起動/停止/再起動・設定編集（テンプレ再生成でコメント保持・「保存して再起動」）・認識ログ・稼働ログ。起動=`Lucas-Dashboard.vbs`/バーの◎。リスナーとは status.json 疎結合
- [x] **グラスバー** `glass_bar.py`＋`web/bar.html`: Siri風リキッドオーブ（音声レベル反応・加算合成3球＋波打つ外周リング）・312x40コンパクト・**タスクバー直上スナップ**（作業領域下端-4px・実測gap 6物理px）・ホバーで≡/◎/✕・履歴パネル上方向展開・NOACTIVATE維持・`[ui] engine="classic"`でtk版へ戻せる
- [x] **pywebviewの罠4連を実測で特定・修正**: ①js_api公開属性のWindowオブジェクト→再帰公開でブリッジ崩壊（`_`隠蔽） ②transparent=True無描画→SetWindowRgnピル形状 ③min_size既定(200,100)の勝手拡張 ④**DPI非認識でコントロール配置ズレ=無描画**→SetProcessDpiAwareness(2)＋物理px SetWindowPos＋ページCSS zoom=scale（まひろのマゼンタスクショが決定打）
- [x] 検証: バー内部オラクル（zoom1.5・ピル=窓ぴったり・オーブalpha描画・タイマーラベル）＋ダッシュボードオラクル（停止中表示・タイル・4タブ・オーブ）＋ロジックテスト10本回帰PASS
- [x] 教訓2件記録: 未較正の代理指標（ピクセル分散）で誤判定→較正の義務化／テスト窓の残骸（マゼンタ事件）→子プロセスTTL watchdog内蔵（demo実装済み）
- [x] ~~★まひろE2E: リスナー再起動→バーの見た目・位置・リキッド~~ → **実機で「見えない」が再発し根治へ（下記 2026-07-13〜14）**

## バー不可視の根治＋トレイ常駐＋アプリ化（2026-07-13〜14 まひろFB「UI見えない/起動できてるか分からない/ダッシュボードどこ」）
- [x] **不可視の真因=DPI二重スケール**: pywebview が create_window でプロセスを勝手にDPI認識化＋WinForms が窓を中心固定1.5倍スケール→旧実装の「物理px指定＋CSS zoom」と二重掛けで座標2.25倍→バーが物理 y=1593（画面外）へ。**修正=DPI認識を自分で設定しない・水平中央/下端アンカーの論理px・zoom1.0・ShowWindow直叩き（UIスレッド飢餓耐性）・_get_hwnd自プロセス限定・glass_bar.logに実測矩形記録**
- [x] **検証手法の是正**: IsWindowVisible/PrintWindow/DOMオラクルは画面外の窓でも成立=可視性の証明にならない→**スクリーンキャプチャ判定に統一**（troubleshooting.md 記録）
- [x] **「起動できなくなった」の真相**: 起動できていた——vbs再クリックが二重起動ガードに弾かれ、それを知る手段がなかった。→ **トレイ常駐アイコン** `tray.py`（pystray・起動と同時に通知領域へ・アイコン有無=稼働有無・右クリック=ダッシュボード/バーをテスト表示/終了）
- [x] **バーをテスト表示**: トレイメニュー or `logs\flash.req` → flash 3秒（セッション外でも必ず表示＝自己診断）
- [x] **dashboard.py をバーと同方式へ**（DPI手動設定廃止・論理px 1040x640・AUMID・WM_SETICON=タスクバーにLucasアイコン）＋**ショートカット**（デスクトップ/スタートメニュー「Lucas Dashboard」→ピン留め可）。アイコン `assets\lucas.ico`（logs\gen_icon.py で再生成可）
- [x] **本番経路の実機E2E（スクショ実証）**: vbs起動→トレイ表示→flash.req→**下端キャプチャにバー実写**（タスクバー直上・緑オーブ）／ダッシュボード visible=True 画面内。残骸speak_server 10個も掃除
- [ ] ★まひろ確認: 通知領域 ^ に Lucas アイコン→右クリック「バーをテスト表示」→Ctrl+Alt長押しPTT

## 声のリッチ化（2026-07-14 まひろ要望「JARVIS系の男声・吹き替え級・不自然じゃないやつ」）
- [x] **「声がなくなった」診断**: SAPI自体は健在（WAV 179KB・RMS 0.11・出力デバイス正常）。真因候補=残骸speak_server 10個の詰まり（掃除済み・`claude mcp list` で lucas-speak √Connected 確認）
- [x] **共有TTSモジュール `tts_engine.py`**: リスナー(Speaker)とspeak MCPの両方が同一経路に。synth（エンジン別）→_play（共通再生・speaking.lock/last_speak_end の契約は従来と同一手順）。**VOICEVOX失敗時はSAPIへ自動フォールバック=声が完全には死なない**（フォールバック経路はエンジン未設置状態で実地検証済み）
- [x] **VOICEVOX導入**: engine 0.25.2 CPU版 1.7GB → `voicevox_engine\windows-cpu\`（Win11内蔵tarが7z対応・14秒で展開）。**初回発話時に自動起動して常駐**（初回38秒・RAM約865MB）。アーカイブは削除済み
- [x] **既定の声 = 青山龍星ノーマル (style=13)**。候補6本を `voice_samples\` に生成（青山龍星ノーマル13/しっとり84/囁き86・玄野武宏11・麒ヶ島宗麟53・剣崎雌雄21）——まひろが聴き比べて config で切替
- [x] dashboard の CONFIG_TEMPLATE / save_config に [tts] を追加（保存で消えない）。ロジックテスト10本 PASS・新声での実再生 spoken 確認・リスナー再起動済み（02:15 待機開始=挨拶は新声）
- [ ] ★まひろ確認: 新しい声の質感（サンプル6本聴き比べ→style変更は言ってくれれば即）
- [ ] **認識が空/幻聴の切り分け（次の本丸）**: 部屋の動画音声をVADが延々拾い transcribing に張り付く実態をログで確認（幻聴「次回予告」等）。マイクデバイス・wake専用軽量ゲート・動画再生中モードを検討

## 瞬時ウェイク＋速度実測（2026-07-14 まひろ要望「軽量モデルでルーカスだけに即反応／文字起こし半分に」）
- [x] **vosk常時ウェイク** `wake_engine.py`（vosk-model-small-ja 48MB・ストリーミング・部分認識で発火）: 「ルーカス」の語尾から**0.2〜0.8秒で発火**（旧=無音1.5s＋whisper5〜15s）。**待機中はwhisper完全停止**＝動画音声の延々認識・CPU張り付き・幻聴履歴が構造的に消滅（認識の空/幻聴問題の根治）。発火→バー即点灯→CC起動裏走→同息指示captureして下書き→声アック（TTSを先に鳴らすとlockが続き発話を捨てる——順序が命）
- [x] **照合方式の実測選定**: grammarモード（語彙絞り）は「おはようございます」まで吸い込む誤発火多発→**自由認識＋トークン完全一致**が正解。緩い変種（ルーカ等）をvosk側に入れるのも誤発火源→[wake] words は「ルーカス」のみで [words] wake と分離。合成10ケース10/10・誤発火ゼロ・RTF0.02
- [x] **結合テスト4本PASS**（偽CC・実vosk・実音声）: 無関係発話無視／発火／CC起動+同息指示下書き／セッション判定。※一括プッシュ給餌はハングする（無音判定はブロック到着時のみ評価）→リアルタイム給餌が必須
- [x] **速度の実測結論（同モデル）**: beam1=+3%のみ・**without_timestamps=Trueは一致率95.2%→42.9%崩壊で禁止**（コメントに焼き込み済み）・cpu_threads=8が最適（論理CPU=8。12/14は逆効果）。体感遅さの真犯人=待機whisperとのCPU競合（同じ15秒が競合下14〜126秒 vs クリーン5.6秒）→ウェイク刷新で解消。さらに上はOpenVINO/NPU化（別トラック）
- [ ] ★まひろE2E: 「ルーカス」→瞬時にバー点灯するか／「ルーカス、〜して」の続きが下書きに入るか／誤発火が実環境で出ないか

## NPU化＋遅延マウント（2026-07-14〜15 まひろGO「NPU使おう・性能維持・メモリ効率」）
- [x] **ハード実確認**: Core Ultra 7 355（Panther Lake）・OpenVINO 2026.2.1 で CPU/GPU/NPU 3デバイス可視
- [x] **モデル**: 公式変換済み `OpenVINO/whisper-large-v3-turbo-int8-ov`。回線0.03MB/s事故→自動レジューム常駐DL（`logs\resume_download.ps1`・本家/ミラー交互）で完走→**SHA256全4ファイル本家一致で改ざんなし証明**
- [x] **NPUベンチ（清浄CPU・171s正解付き）**: short15s **0.4s**（CPU5.9s→14倍）/ long **9.8s RTF0.06**（63.5s→6.5倍）/ **一致率95.1%維持** / キャッシュ済み再ロード**2〜4s**（初回コンパイル26.7分は1回きり・`models\ov_cache` 2GB）
- [x] **統合**: `[stt] engine="openvino" device="NPU"`・失敗時faster-whisper自動フォールバック・OpenVINO時はfwを一切ロードしない（二重メモリ回避）・`return_timestamps=True`必須（長尺精度の教訓を移植）
- [x] **遅延マウント（まひろ案）**: 認識は「まとめて」方式なので構造的に成立。`unload_idle_min=15` でアイドル解放（**実測: del で約950MB返却**）→ウェイク発火/PTT押下で先読み復帰（2〜4s・`preload_stt`）→transcribe冒頭の `_ensure_stt` が最終保険。復帰失敗時はfwロードで認識を死なせない
- [x] **本番検証**: NPUロード3.3s・ウォームアップ1.0s・**まひろ実声「よろしく」認識0.4s→CC送信までライブ成功**（00:20）。ロジックテスト10本PASS
- [x] **バーの絶対最前面固定（2026-07-15 まひろ指示）**: 表示中0.5s毎に「自分より上に可視の重なり窓（DWMクローク除外）」を走査→居たら NOTOPMOST→TOPMOST→HWND_TOP で帯の先頭へ再挿入。**敵対テスト**（マゼンタのtopmost窓をバー位置に配置→バーが上に乗る実画面スクショ）で合格。落とし穴=GetWindow等の argtypes 未指定は64bit HWND切り捨てで検知が沈黙（1回目の敗北の真因）
- [x] **バーをタスクバー非表示に（2026-07-15 まひろ指示）**: 真因=WinFormsが立てる **WS_EX_APPWINDOW（タスクバー強制表示）が TOOLWINDOW より勝つ**。APPWINDOW剥がし＋FRAMECHANGED を `_apply_noactivate` に恒久化。稼働中のバーには外科的にライブ適用（再起動なし=下書き14件無傷）し、**バー表示中にタスクバーへボタンが出ない**ことをスクショで実証
- [x] **人声ゲート（2026-07-15 まひろ要望「雑音を文字起こしするな」）**: Silero VAD（2.3MB・onnxruntime CPU）を whisper 手前の関門に。判別テスト6/6（人声0.49=音量15%でも不変／雑音・打鍵・ハム・低域 全て0.00）→閾値0.1。対話＋同息指示に適用（PTTは意図的操作なので対象外）。全発話の speech= 値をログに残し閾値調整可能。**罠2つ**: ①Silero は context 64サンプル連結必須（無いと実声も0点） ②OpenVINO では変換不可→onnxruntime
- [x] **下書き消しUI（2026-07-15）**: バーの「下書きN」バッジをクリック→全消し（ホバー赤・UIState.clear_pending_requested 経由・_capture_utterance が消費）。旧リスナーに37件溜まっていた実害を確認
- [x] **JARVISモード（2026-07-15 まひろ要望）**: 音声セッション専用の人格を `CLAUDE.md` に定義（呼称サー・執事調・静かなウィット・先回り。グローバルのルーカス口調より優先・規律は維持）。声のfx="jarvis"と統一
- [x] **誤診の是正（記録済み）**: 「VOICEVOXが無音」はPSパイプ経由コードの日本語リテラルcp932化けが真因＝エンジンは終始健全（troubleshooting.md 記録・日本語含むコードはファイル経由実行を義務化）
- [ ] 観察: 15分アイドル後に「STT解放」ログ→RAM実測降下の確認／NPU長期安定性（数日）
- [ ] （次段階候補）話者照合ゲート＝まひろの声だけ通す（動画音声対策の本丸）
- [ ] （必要なら）int4モデル比較=さらに省メモリ・GPUデバイス比較

## バーUI全面改装＝正方形オーロラ粒子タイル（2026-07-15 まひろ指示・実装＋本番反映）→ **同日深夜FBで v2 に全面改訂（次セクション）。粒子・虹・DWMアクリルは廃止**
- [x] **要件**: 半透明リキッドグラス／背景は透明／右下タスクバー直上／正方形／オーブ→大量の粒子が音で形を変える・オーロラ多色を虹状に色セグメント配置・タイルの1/2サイズ／文字は下に小さく（既存機能は全部温存）
- [x] **実装**: `web/bar.html` 全面改装——フィロタキシス配置の粒子1100個・オーロラ6色を粒子の固有角度で6セグメント割当（境界±15%は確率混色で柔らかい継ぎ目）・音声レベルで5弁の花に変形（r *= 1+amp·u·sin(5θ+t)＝中心安定・外周ほど開く）・処理中は渦（内側ほど速い回転）・状態別パラメータ表 MODES・active無音時30fps・重い環境では粒子を自動間引き（下限400）。`glass_bar.py`——200x200 右下アンカー（MARGIN_R=10）・角丸は CSS 24px と SetWindowRgn を同径換算で一致（CORNER_CSS=48）
- [x] **真の半透明（DWMアクリル）**: プロセス起動前に `WEBVIEW2_DEFAULT_BACKGROUND_COLOR=00000000` ＋ `DwmExtendFrameIntoClientArea(-1)` ＋ `DWMWA_SYSTEMBACKDROP_TYPE=3`。失敗時は poll の glass=False → bar.html が不透明ダークグラスを自前描画（どちらに転んでも不可視にならない二段構え）。ダッシュボード子プロセスには透過 env を渡さない
- [x] **検証（実画面キャプチャ・デモ45s×2回）**: 右下配置 actual=(1605,822,300x300)＝タスクバー直上／録音中の花型変形／処理中の渦巻き／下書きバッジ／show-hide／良心カード（topmost）より上に勝つ patrol。**アクリルは背景差分で実証**——同じタイルが暗い背景では濃紺・白い背景ではスモークグレー（不透明窓なら背景に関わらず同一画素のはず）
- [x] **本番反映（18:17）**: 旧リスナーは 15:36 に無言死していた（自己ログに死因なし・「人声ゲートで破棄」の通常行で途絶。下書き28件はプロセスと共に消滅済み）→ 残骸 `logs\rival_topmost.py`（0:39から17時間・前セッションの最前面テスト窓の消し忘れ）を掃除 → 新リスナー起動 → `flash.req` 自己診断で新タイルの本番表示をスクショ実証・glass=acrylic
- [x] 旧ピルUIのバックアップ: `web/bar_pill_backup.html`（戻し方コメント付き。このフォルダは git 管理外のため明示退避）
- ~~★まひろ確認~~ → FB受領（「オーブ気持ち悪い・サイケすぎ・球体で波打つ洗練に」「グラスは透明・iOSのひずみ」）＝ v2 へ

## バーUI v2＝Siriスフィア＋DIY透明グラス（2026-07-15深夜〜16 まひろFB反映・実装＋本番反映済み）
- [x] **オーブ全面差し替え**: 粒子・虹を廃止 → **Siri風リキッドスフィア**。基本は真円の球体・声で縁が液面リップル（k=3/5/7 の正弦合成・無音時はほぼ静円）・寒色3色ブロブ（アイスシアン/インディゴ/バイオレット）が内部で旋回して滑らかに溶け合う・白熱コア・処理中は旋回アーク・speaking は自己脈動。球の体は rgba(7,12,28,.74)＝派手な背景でも色格を守る
- [x] **DWM透過の全滅を実測確定（Win11 26200）**: SBT3 明/暗とも黒地・accent4=黒地・accent2=**ウィンドウ内容ごと不可視**・**SetWindowRgn×DwmExtendFrame 併用=ウィンドウ全消し**。→ OS任せの透過は打ち切り
- [x] **DIYグラス（自前サンプラ）で真の透過**: `_backdrop_sampler` がバー背後を 0.6s 毎スクショ→1/3縮小→GaussianBlur(6)→JPEG base64→`setBackdrop()` push（md5同一ならskip＝静止時ほぼゼロコスト・表示中のみ稼働）。**採取の瞬間だけ WDA_EXCLUDEFROMCAPTURE**＝幻影ループ根絶かつ普段のスクショにはバーが写る。Pillow死亡→glass=False→不透明ダークグラス（不可視にならない二段構え）
- [x] **iOSの縁の屈折（ひずみ）**: 背後実画像に feDisplacementMap（縁38%帯だけ内向きバルジの自前変位マップ・scale=34）＋虹彩リム＋白薄膜（#glassfilm）＋下部スクリム＝リキッドグラスの材質
- [x] **検証（実写・デモ）**: カラフルTOPMOSTストライプ窓を背後に敷き、**タイル越しにぼかされたストライプが透ける**ことをスクショ実証。球の色格・下書きバッジ・ラベル可読・show/hide も確認
- [x] **本番反映（00:37）**: pending=0 を確認して再起動 → flash.req 自己診断 → 本番スクショで新タイル実証
- [x] **GUI検証の環境法則を確立**（fact / troubleshooting 記録）: ①CCツールの bg 実行=分離デスクトップでGUI不可視 ②ツールプロセス内生成のGUIも不可視 ③**Start-Process 切り離しなら実デスクトップに出る** ④全画面 topmost アプリ環境でのテスト窓は TopMost+NOACTIVATE 必須
- ~~★まひろ確認~~ → FB受領（「磁性流体スピーカーの完全再現・リアルな液体感・色は選択制・タイル少し小さく」）＝ v3 へ

## バーUI v3＝磁性流体フェロフルイド（2026-07-16朝 まひろFB・実装＋本番反映済み 09:50）
- [x] **要件**: 添付画像（磁性流体スピーカー）の磁性流体を完全再現／リアルな液体感が最重要（音ではじける・認識中は波打つ）／色は選択制／タイルを少し縮小
- [x] **メタボール実装**: キャンバスには不透明の円を描くだけ → CSS `#goo` フィルタ（ぼかしσ6→アルファ閾値22/-11）が円同士を表面張力のように融合＋`drop-shadow`は閾値後の液体形状に落ちる＝立体感。構成=本体1＋腕5＋衛星滴（参考画像の有機スプラット）
- [x] **音反応**: レベルのアタック検出→滴を発射し、バネ+重力+減衰で本体へ戻り再融合（**はじける**）／transcribing系=腕の進行波（**波打つ**）／speaking=拍で弾む+飛沫／声で腕が伸びる（stretch）
- [x] **光沢**: 別キャンバス `#gloss`（gooの閾値に光沢が巻き込まれるため分離必須）。明色流体では自動で控えめ（輝度>0.6でgloss 45%）
- [x] **色選択制**: config `[ui] fluid_color`（既定=黒鉄 #0b0d14。プリセットコメント: 深藍/紫紺/深紅/白銀）→ poll経由でJSへ。**dashboardテンプレ同期＋往復テストで「保存しても消えない」を実証**
- [x] **副次修正**: dashboard CONFIG_TEMPLATE に `[tts] fx` が欠落＝設定保存で fx="jarvis" が消える潜在バグを発見・修正（往復テストに含めて実証）
- [x] **タイル縮小**: 200→**172**（CORNER_CSS 48→44・CSS --radius 22px・流体ジオメトリはタイル実寸から自動再計算 R0=幅30%）
- [x] **gooの罠（実測）**: σ未満の小円は閾値で消滅する——初版で衛星滴(2-3px)が全滅→4px以上に。腕は本体の外（dist 0.52R〜）に出さないと輪郭に出ず卵になる
- [x] **検証**: ストライプ実写でスプラット/波打ち/光沢/透過を確認 → 本番反映（09:50 flash実写=白背景に黒流体＋透け、参考画像の質感）
- [x] **v3.1 完全透明化（同日夕 まひろFB「完全に透明・iOS Liquid Glass級のひずみ・Psアイコン参考でそれ超え」・本番反映 18:33）**: サンプラぼかし σ6→2（1/2縮小・q80）＝背後が素通しで読める／白膜・accentティントを全廃（上辺スペキュラ極薄のみ）／変位マップを **squircle距離場＋外向き法線**に刷新し、縁30%帯を内向きに折り込む（scale 40→**90**。内向きサンプリング=画像外参照が起きず破綻しない）／**R/Bチャンネルゴースト2枚（scale 118/64・縁リングマスク・screen合成）＝プリズム色分散**／白リムをクリスプに強化。検証=ストライプ実写（素通しの透け・縁の強い折れ曲がり）＋本番flash実写（Looker画面のテキストが透けて縁で曲がる）
- [x] **v3.2 一枚ガラス化（同夜 まひろFB「ぼかしいらん・もっとリキッドに・変な枠を消して完全に1枚のUIに・ひずみは縁ほど強く」・本番反映 20:10）**: サンプラ完全無加工（フル解像度・ぼかしゼロ＝真の素通し）／「枠」の主犯=虹彩リム #rim を撤去・縁光は極薄ヘアライン2本と境界線.22のみ＝**屈折そのものが縁の表現**／変位カーブを連続ランプ化（中心70%はほぼ素通し→縁で t³ 急増・最大約50px折り込み）＋角度方向の有機ゆらぎ＝吹きガラスのリキッド感／分散ゴーストはデルタ縮小（scale 114/86）・マスク広め／検証=ストライプ実写（縁の折り込み・有機うねり）＋本番flash実写（明るいUI上で文字が透けて縁で曲がる）。※フラッシュ3秒 vs snap.ps1 のC#コンパイル遅延で6連ミス→「撮影を先に起動→flash連打」の順序が正解（バッテリー駆動時は特に）
## 磁性流体 v2＝自由パーセル物理（2026-07-16夜 まひろ指示・本番反映 21:20）
- [x] **「定形の球体をやめ、液体には中心にいようとする力だけを与える」**（まひろ指示）: 本体1＋腕5の固定構造を全廃 → **自由パーセル14粒**。各粒に与えるのは ①中心向きの力 ②近接反発（一点に潰れない最小限） ③粘性減衰 ④ゆらぎ、だけ。**形は力の均衡から創発**し、音のアタックで粒ごと弾け飛び、中心力で戻ったところを goo が再融合＝本物のはじけ
- [x] **中心力は「中心に近いほど強い」逆プロファイル**（まひろ追加指示。grip = 2600·(d/(d+3))·(L/(L+d))、L=0.55R0）——中心の塊はきつく握られ、遠くへ弾けた粒ほど自由に漂って帰る
- [x] バランス調整の実測: 初版はグリップに反発が負けて小さな卵に圧縮 → 反発 1500→3000・到達半径 0.62→0.70 で「タイル約半分の有機塊」に復元。波打ち力 430→560
- [x] 光沢は液塊の重心に追従し、飛散中は自動減光（cohesion 連動）。稀に塊内に小穴（泡）が開くのは液体らしさとして許容
- [x] 検証: ストライプ実写3態（うねる翼状/丸塊/三つ葉＝毎ショット別形状で「定形なし」を確認）→ 本番flash実写
- [x] **凝集チューニング（同夜 まひろFB「中心力弱い・ひっつき力も・はじけすぎるな・画像みたいに」・本番反映 21:47）**: ①中心力 2600→**4200**（L=0.50R0＝中心は強く握るが広がりは殺さない） ②**粒間ひっつき力（表面張力）を新設**——山形の引力帯。**帯を自然間隔に重ねると常時圧縮になり豆に潰れる（2回実測）→ 帯を rr×1.3〜2.6 の外側にずらし「離れた粒の回収」専用が正解** ③はじけ抑制: キック 110+300→80+190・発火閾値 0.09→0.11・飛沫減・拍420 ④反発 4400・到達 0.85・ゆらぎ150・粒径+12% → 静止形=一体の有機ローブ塊（三つ葉/翼/四つ葉が緩やかに変形）
## ガラス強化＝広域ひずみ＋縁全廃（2026-07-17未明 まひろFB「歪弱すぎてガラス感ない・縁はいらない」・本番反映 00:10）
- [x] **変位ランプ広域化**: 素通しは中心12%だけ→t^2.2 で広域に立ち上げ・scale 100→**140**（縁の折り込み最大約70px）／レンズ拡大 1.05→**1.12**（厚ガラスの膨らみが全面に）／色分散 160/122・帯を内側へ拡大・opacity .5
- [x] **縁の完全撤去**（グラスモード）: 境界線=transparent・ヘアライン=none——境界は屈折そのものが語る
- [x] **liquid-glass-react（まひろ提案）をソース精読**: 同一系統の技法（feDisplacementMap＋生成変位マップ＋R/G符号化＋チャンネル分割の色収差）と確認。差分は falloff 曲線（smoothstep vs べき乗）と変位方向（放射 vs squircle法線）程度 → **React依存を足す価値なし・本質は移植済み**と判断（falloffのsmoothstep化は好みのノブとして提案可）
- [x] 検証: ストライプ実写（タイル全域の歪み・枠なし・黄帯の大湾曲）→ 本番flash実写（角で背後の白が折り込まれる）。※ブートが重負荷時14秒に伸びて撮影が全ミス→待ち13秒に修正（snap先行起動+flash連打の教訓は継続）
## リアルタイム透過＋立体感＋2/3縮小（2026-07-17朝 まひろFB「歪んだ画像貼り付けみたい・動かすとラグ・本当に透けて・のっぺり・フチいらん・でかすぎ」・本番反映 09:42）
- [x] **リアルタイム化**: サンプラ 0.6s→**70ms間隔**（実測ベンチ: 1周39ms＝**実効14fps**。変化時のみ転送＝静止時は軽いまま）。**動くストライプ（120px/s）での実写でタイル内外の帯がほぼ整列**＝旧版の「貼り付け」感の正体（0.6sラグ）が消えたことを画像で実証
- [x] **本質の整理（まひろの質問「本物できないの？」への技術回答）**: 屈折つき透過は Windows ではどのアプリも「背後画素を読む＝キャプチャ」しかない（DWM合成器が非公開。Appleは合成器内蔵だから本物）。素通しだけなら per-pixel alpha で遅延ゼロの本物が可能だが**屈折を失う**。→ 現路線=高速キャプチャ合成が正解。さらに上は DXGI Desktop Duplication（dxcam・30-60fps）が次弾
- [x] **立体感**: 上面の曲率光＋下部の厚み陰（線を作らない広いグラデのみ＝フチ無し規律維持）
- [x] **残フチ掃除**: 下部スクリム帯を撤去（ラベルは強text-shadowで可読）
- [x] **サイズ 2/3**: 172→**116**（物理174px実測）。角丸16px・goo σ=W×0.035 で自動追従・バッジ/ボタン/ラベル縮小
- [x] **dxcam(DXGI)化で本物のライブ透過（2026-07-17 まひろ「70msより上げれない？」）**: 実測1.7ms/フレーム＝ソース更新レート（〜50fps）まで追随。cv2依存を踏み BGRA生取り+numpy並べ替えで回避。**副作用: dxcam中はWDA常時除外＝バー表示中はスクショ/録画にバーが写らない**（非表示で解除・検証は LB_NO_DXCAM=1 + LB_PACE=0.5 ノブで）。テカり=スペキュラを流体の上(z4)へ・黒線の犯人=下部黒ビネット除去・中心核ピン留め＋近傍ロック項

## バーUI v4＝レンズアイ HAL 9000系（2026-07-18〜19 まひろ判断「磁性流体は退役・カメラレンズ型の目へ」・本番反映 07-19 05:42）
- [x] **退役とバックアップ**: 磁性流体一式は `web/bar_ferrofluid_backup.html` へ完全退避（復元=コピーバック+リスナー再起動。fluid_color 設定も互換残置）
- [x] **レンズアイ実装**: 金属バレル（上方光の円環＋段差リング）＋暗い筒の底＋**ガラスドームのスペキュラ**（大グリント＋弧リフレクション2本）＝レンズらしさの決め手。中心に**球体グラデの光彩核**＋放射ストライエーション36本（絞りの質感）＋発光ブルーム
- [x] **音声反応**: 瞳孔 Rp=Ri·(0.24+0.30·lvl)・光彩開度も連動＝**声で瞳孔が拡大縮小**。発話中は拍でやわらかく明滅
- [x] **状態表現**: 待機/対話=機械シャッターの瞬き（2.8〜7.3s ランダム・2割で二度瞬き・smoothstep 0.26s）／認識・処理中=光彩内をスキャンアーク周回（残像3本）／外周ハロー=accent色（状態色）
- [x] **スキン4色** config `[ui] eye_skin`: **skyblue**（スカイブルーグラデ・既定）/ **unicorn**（ドリーミーパステル虹・conicゆっくり回転）/ **hal**（2001年宇宙の旅オマージュ赤）/ **aurora**（ティール×紫）。dashboardテンプレ同期＋往復テストで保存耐性を実証
- [x] 検証: ゲート全緑（py compile×2・config parse・設定往復・JS syntax）→ 実写（バレル/光彩/瞳孔/グリント/ストライエーション・下書きバッジ/ラベル共存・グラス透過と両立）→ 本番反映（dxcamリアルタイム透過と共存・ログ健全）
- [ ] ★まひろ確認（実声で）: 瞬きの間隔と気配・瞳孔の声反応・スキンの好み・スキャンアークの見え方
- [ ] （調整ノブ）瞳孔レンジ(0.24+0.30)・瞬き間隔/速度・スキンの色stops・レンズ外径 R0=幅30%——全部定数一箇所

## バーv4.1＝カメラレンズのベゼル＋カラー瞳孔（2026-07-19 まひろ指示「縁をカメラのレンズみたいに・瞳孔は黒でなく青→白グラデ・赤にも設定から変えれるように」・本番反映 12:55）
- [x] **カメラレンズのベゼル**: 旧・単純バレルを多段構造に置換——外周ベベル光→**マシンドメタル環**（conic旋盤目＝周方向明暗ゆらぎ＋ローレット刻み56本）→溝→**内向き斜面**（逆光=奥へ落ちる筒）→口金リング→ドーム。RD=RL×0.80→0.76でベゼルを太らせ、R0=幅30%→31%
- [x] **瞳孔=白→色のグラデ発光核**: 黒瞳孔を廃止。白コア（上左寄り=球のハイライト）→mid（色+白55%）→base（設定色）→deep（縁の落ち際）の球体グラデ＋同色グロー＋明環。パレットは色変更時のみ再計算
- [x] **config `[ui] pupil_color` 新設**（既定=青#4fa8f5。コメントに赤#ff4d4d/金#ffc24d/紫#b78aff/翠#4de0b8）: glass_bar.py（BarApi._pupil→poll d["pupil"]→config読込）＋ dashboard設定画面テンプレ同期（保存で消えない=往復テストにassert追加）
- [x] 検証: ゲート全緑（py_compile×2・config parse・往復・node --check）→ **実写2周**: 既定=青白瞳孔（発話中/対話中）＋ **config を赤#ff4d4dに変えてデモ→赤瞳孔を実写確認**（=配管E2E証明）→青に復元 → 本番再起動（pending=0確認済み・dxcam有効・flash SHOW/HIDE健全）
- [ ] ★まひろ確認: ベゼルのレンズ感・青白瞳孔の見え方・（好みで）pupil_color の色変え——configかダッシュボード「保存して再起動」

## バーv4.2＝色は中心コア球だけ・ダッシュボードから色変更（2026-08-06 まひろ指示「中心の玉だけ色がついてる感じに・周りのにじみは消して・ダッシュボードから変えれるように」・本番反映 13:30）
※日付注記: この開発機の時計が長く遅れており、2026-08-06 13:00頃に補正されて表示が 07/19→08/06 へジャンプした（実測: タスクバー時計＋セッション日付の突合）。**v3〜v4.1 の見出し日付（07-16〜07-19）はズレた時計由来**で、実日付はおおむね +18日（08-03〜08-05）と推定される。時刻（時分）は正しい。
- [x] **にじみの撤去**: 旧・光彩の色面（スキン配色の大円）と広域ブルームを削除し、レンズ内部を無彩色化（同心リング2本＋淡い白の絞りスジ36本＋白のスキャンアークのみ）。色を持つのは中心のコア球ひとつ＝HAL 9000 の構図に接近
- [x] **コア球**: 半径 RL×(0.17+0.17·lvl)＝声で拡縮（旧瞳孔より大きく主役化）・白コア→設定色→深色の球体グラデ・**接触グローは×1.45で打ち止め**（周囲へにじませない）・縁の明環・ガラスのハイライト点
- [x] **色ノブを1本化**: 旧 `[ui] eye_skin`（光彩スキン4種）を廃止し `[ui] pupil_color` へ統合。16進色 or `"rainbow"`（旧unicorn虹はコア球へ継承・conic回転・彩度を上げて小径でも虹と分かる値に調整）
- [x] **ダッシュボードUI**（指示の本丸）: 設定 →「レンズアイ（バーの目）」カード＝**実物と同じ導出のCSSグラデでライブプレビュー**＋プリセット7色（青/赤HAL/金/紫/翠/白/虹）＋カラーピッカー＋16進直接入力。「保存して再起動」でバーへ反映
- [x] 検証: ゲート全緑（py_compile×2・config parse・保存往復＝eye_skin撤去とpupil_color保持をassert・node --check×2）→ 実写: 青コア（対話中/起動中）と虹コア（発話中）でにじみ皆無を確認 → ダッシュボードは Edge ヘッドレスで実描画（青・虹の両方でプレビューとスウォッチ選択状態を確認）→ 本番反映 13:30（dxcam有効・flash SHOW/HIDE健全）
- [x] **事故と修復**: config.toml を PowerShell `Set-Content -Encoding utf8` で書いて **BOM混入 → tomllib パース不能**（バーが既定色へフォールバックし「虹にならない」で発覚）。BOM除去して修復。以後 config 書き換えは Python で BOMなし＋書いた直後にパース検証する専用スクリプトに統一（教訓 LL-100）
- [ ] ★まひろ確認: にじみが消えたか・コア球の大きさと声への反応・好みの色（ダッシュボードで即変更可）

## バーv5＝ガラスを skill 準拠＋実光学へ／GitHub 管理開始（2026-08-06 まひろ指示・本番反映 14:2x）
リポジトリ: https://github.com/menma22/lucas-voice （**private**）。models/ voicevox_engine/ .venv/ logs/ voice_samples/ は .gitignore で除外（再取得可能・計約5.7GB）。改行は .gitattributes で LF 固定（既定 autocrlf=true だとコミット直後から常に modified に見えた）。
- [x] **屈折を実光学に置換**（まひろ指摘「自分たちの適当な値でやるな。歪みすぎ・変に反転している」）: 出典 kube.io "Liquid Glass in the Browser"。squircle 凸ベベルの断面 → 法線 → スネルの法則(n=1.5) → 厚み14pxぶんの横ずれ。ノブは **ベベル幅13px / 厚み14px / 屈折率1.5** の物理量のみ（旧: 距離場の2.4乗＋目分量 scale=130）
- [x] **参照方向を外向きへ**: 旧版は内向きに折り込んでいたため背景が反転・混合して見えていた。実物のガラス同様、縁は外側の景色を寄せて見せる。最外周2.5pxは変位を0へ戻す（窓の外の画素はサンプラが持っていないため）
- [x] **中心は変位ゼロ＝背景が1:1で素通し**（まひろ指示「ほぼクリア／後ろに透けてるやつはだいたいそのまま」）。地色 alpha .07・ぼかし無し
- [x] 背後画像を**窓全体に1:1**で敷き直し（旧: タイル内に object-fit:cover で押し込み＝パネル展開時にズレていた）
- [x] skill（emilkowalski/skills: apple-design §12 / emil-design-eng）準拠: 光を受ける明るい上縁・ラベルのヴァイブランシー（weight500/字間+.04em/密着影＋広い薄影）・バッジは不透明面に色・パネルは厚いマテリアル(blur16px)・開閉は materialize(開220ms/閉150ms)・`transition: all` 廃止＋自前イージング・`:active` 押し込み・ホバーは (hover:hover) で囲う・reduced-motion / -transparency / contrast 対応
- [x] 撤去: R/Bプリズムゴースト・手描きスペキュラ膜・ノイズ層・未使用の goo フィルタ
- [x] 検証: 同一背景（固定パターン）を敷いた実機キャプチャで改修前後を比較（LL-008: 可視性・見た目は画面合成結果で測る）。node --check 通過
- [ ] **既知の不具合（今回の変更とは無関係・要修正）**: 履歴パネルが開かない。JS を経由せず `set_expanded(true)` を直叩きしても窓が伸びず `innerHeight` は 116 のまま＝Python 側の展開処理の問題
- [ ] ★まひろ確認: 実機で歪みの量が好みか（強くするなら initLens の THICK、帯を広げるなら BEZEL）

## Step 4 — 常駐化＆使い勝手
- [ ] ★まひろE2E（新ビルド・再）: ヘッドレス起動（バーは出ない）→「ルーカス」→バー出現→CC起動（lucas-voiceで開くか）→発話→「これでOK」送信→**CCが毎ターン喋るか**→「バイバイ」→バー消滅
- [ ] スタートアップ登録（E2E合格後）
- [ ] 誤検出調整（wake誤認識バリエーション採集）
- [ ] （不満あれば）VOICEVOX / kotoba-whisper v2.2 / beam速度調整

## メモ / 設計変更
- VAD は当初 silero-vad 想定 → MVP は音量ベース簡易VAD（torch 不要）に軽量化。精度不足なら silero へ。
- GPU 無し（Intel iGPU）→ STT は CPU 推論。small/medium から、精度を見て turbo/kotoba-whisper。
